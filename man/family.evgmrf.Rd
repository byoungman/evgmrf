% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{family.evgmrf}
\alias{family.evgmrf}
\title{Supported Families for evgmrf Models}
\description{
Documentation for the error distributions and data structure expectations
supported by the \code{\link{evgmrf}} fitting function.
}
\details{
The following families are available:
\itemize{
\item \code{"gev"}: Generalized Extreme Value distribution for block maxima.
\item \code{"gpd"}: Generalized Pareto distribution (GPD) for peaks-over-threshold data.
\item \code{"poisgpd"}: Point process GPD model. Requires either \code{args$r} or \code{args$u}.
\item \code{"rlarge"}: $r$-largest order statistics model. The response data \code{z}
must be a 3D or 4D array depending on grid form.
\item \code{"ald"}: Asymmetric Laplace distribution. Requires \code{args$tau}.
\item \code{"poisproc"}: Poisson process model.
}

For the Asymmetric Laplace distribution, \code{args$tau} specifies the quantiles
to be estimated. For example, \code{args = list(tau = .5)} gives a median estimate.

For the point process model, \code{args$r} specifies the number of largest order
statistics to use and \code{args$u} specifies the threshold above which values
are used.
}
\examples{

\dontrun{
data(COtop5prcp)
m_rlarge <- evgmrf(COtop5prcp$prcp, family = 'rlarge')

COmxprcp <- COtop5prcp$prcp[, 1, , ]
m_gev <- evgmrf(COmxprcp, family = "gev")

# Estimate 90th percentile
m_ald <- evgmrf(COmxprcp, family = "ald", args = list(tau = .9))

data(COexcprcp)
m_gpd <- evgmrf(COexcprcp$prcp, family = 'gpd')

data(COprcp20)
m_pp <- evgmrf(COprcp20$prcp, family = 'poisgpd', args = list(u = 20, nper = 56))
}

}
\seealso{
\code{\link{evgmrf}}
}
\keyword{datasets}
